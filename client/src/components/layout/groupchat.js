/*

import { Children } from "react";
import react from 'react';
import {Modal} from "@charkra-ui/react"
import {ModalOverlay} from "@charkra-ui/react"
import {ModalContent} from "@charkra-ui/react"
import {ModalHeader} from "@charkra-ui/react"
import {ModalCloseButton} from "@charkra-ui/react"
import {ModalBody} from "@charkra-ui/react"
import {ModalFooter} from "@charkra-ui/react"
import {Button} from "@charkra-ui/react"
const GroupChatModel=({Children})=>{
    const{isOpen,onOpen,onClose}=useDisclosure()


    return(
        <>
        <Button onClick={onOpen}>Open Model</Button>
        <Modal isOpen={isOpen} onClose={onClose}>
            <ModalOverlay />
            <ModalContent>
            <ModalHeader>Modal Title </ModalHeader>
            <ModalCloseButton />
            <ModalBody>
                <Lorem count={2} /> 
            </ModalBody> 
            <ModalFooter>
                <Button colorScheme="blue" mr={3} onClick={onClose} > Close</Button>
                <Button variant="ghost"> Secondary action</Button>
            </ModalFooter>  
            </ModalContent>
            </Modal>
        <div></div>
        </>
    
    

)
}

export default GroupChatModel;
*/

/*
<GroupChatModel>
<button d="flex"
fontSize={{base:"17px",md:"10px",lg:"17px"}}
rightIcon={<AddIcon />}
>New Group Chat</button>
</GroupChatModel>
<br />
*/